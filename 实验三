 #include <stdio.h>
#include<string.h>
void main()
{
char  a[100];
char b[10];
gets(a);
gets(b);
int s=strlen( a);
int w=strlen(b);
int n=O;
for(int i=0;i<=s-w;i++)
{
char t[w+1];
t[w]='\0';
for(int j=0;j<w;j++)
t[j]=a[i+j];
if(strcmp(t,b)==0)
n++;
}
printf("\n%d\n",n);
}
